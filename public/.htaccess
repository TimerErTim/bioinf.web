RewriteEngine On

# If the URL is not a real file (css, images) or folder, send it to index.php.
# This enables clean URLs like /quotes/5 instead of /index.php?route=...
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

RewriteRule ^ index.php [L,QSA]
